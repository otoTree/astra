# 共绩只读合同夹具

这些响应由 `docs/providers/gongji/api/` 中 2026-08-19 的 OpenAPI 示例裁剪而成，只保留阶段 8-9 使用字段，并替换任务名、区域、镜像和资源标识。它们不包含真实凭证、用户数据、预签名 URL 或仓库密码，也不会触发网络请求。

当隔离环境取得供应商授权后，可以追加真实响应的脱敏录制版本；录制前后都必须运行递归敏感字段检查，并保留来源文档版本和响应哈希。不得用录制夹具替代运行时未知字段隔离。
