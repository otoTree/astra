{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "astra.serviceAccountName" . }}
  labels:
    app.kubernetes.io/name: {{ include "astra.name" . }}
{{- end }}
